package exercicios.streams;

import java.util.*;
import java.util.stream.*;

/**
 * =============================================
 * EXERCICIO 13 - STREAMS E LAMBDAS
 * =============================================
 * OBJETIVO:
 * Praticar expressoes lambda, pipelines com Stream API e uso de Optional.
 *
 * O QUE A ATIVIDADE PEDE:
 * 1. Lambdas basicas:
 *    - ordene, percorra e filtre listas com lambdas;
 *    - crie Comparator com lambda e method reference.
 * 2. Stream API com strings:
 *    - filtre nomes por inicial;
 *    - transforme em maiusculas;
 *    - ordene por tamanho;
 *    - limite a quantidade;
 *    - encadeie tudo em uma unica pipeline.
 * 3. Stream API com objetos:
 *    - filtre produtos;
 *    - agrupe por categoria;
 *    - calcule media, maximo, soma e mapa de nomes por categoria.
 * 4. Optional:
 *    - trate resultado ausente com findFirst();
 *    - use orElse(), orElseGet(), orElseThrow(), map() e flatMap().
 *
 * PARA REVISAR:
 * - map transforma, filter filtra e reduce agrega.
 * - Streams nao modificam a colecao original.
 */
public class Ex13_StreamsELambdas {

    // Crie a classe Produto aqui

    public static void main(String[] args) {
        // TAREFA 1

        // TAREFA 2

        // TAREFA 3

        // TAREFA 4

    }
}
