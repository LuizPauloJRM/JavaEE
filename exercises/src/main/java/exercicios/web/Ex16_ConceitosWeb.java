package exercicios.web;

/**
 * =============================================
 * EXERCICIO 16 - CONCEITOS DE WEB
 * =============================================
 * OBJETIVO:
 * Simular a estrutura de uma API REST de tarefas sem usar framework.
 *
 * PARA REVISAR:
 * - Metodos HTTP principais: GET, POST, PUT, PATCH e DELETE.
 * - Status comuns: 200, 201, 204, 400, 404 e 500.
 * - REST usa recursos em URLs, comunicacao stateless e nomes por substantivos.
 * - Em MVC, model concentra dados e regras; controller orquestra a request.
 *
 * O QUE A ATIVIDADE PEDE:
 * 1. Modele a API:
 *    - crie Tarefa, HttpMethod, HttpStatus, ApiResponse, TarefaRepository e
 *      TarefaController;
 *    - use HashMap como banco em memoria;
 *    - retorne o status correto para cada operacao;
 *    - trate 404 para id inexistente e 400 para dados invalidos.
 * 2. JSON manual:
 *    - crie uma conversao simples de Tarefa para JSON;
 *    - crie o caminho inverso de JSON para Tarefa sem biblioteca externa.
 * 3. Demonstracao:
 *    - simule POST, GET, PUT e DELETE no main;
 *    - imprima status e corpo das respostas.
 */
public class Ex16_ConceitosWeb {

    // Crie suas classes aqui

    public static void main(String[] args) {
        // Simule chamadas a API

    }
}
