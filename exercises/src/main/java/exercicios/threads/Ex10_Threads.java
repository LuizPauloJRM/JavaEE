package exercicios.threads;

/**
 * =============================================
 * EXERCICIO 10 - THREADS
 * =============================================
 * OBJETIVO:
 * Praticar criacao de threads, sincronizacao, comunicacao entre threads e uso
 * das APIs de concorrencia do Java.
 *
 * O QUE A ATIVIDADE PEDE:
 * 1. Criacao de threads:
 *    - crie uma thread herdando de Thread;
 *    - crie outra implementando Runnable;
 *    - observe a execucao intercalada.
 * 2. Race condition:
 *    - faca duas threads incrementarem o mesmo contador;
 *    - observe o resultado errado sem sincronizacao;
 *    - corrija com synchronized.
 * 3. Produtor/consumidor:
 *    - compartilhe um buffer limitado;
 *    - coordene produtor e consumidor com wait() e notify().
 * 4. Thread pool:
 *    - use ExecutorService com 3 threads para executar 10 tarefas.
 * 5. Callable e Future:
 *    - execute tarefas que retornam resultado;
 *    - recupere os valores com Future.get() e monitore com isDone().
 *
 * PARA REVISAR:
 * - Diferenca entre Thread e Runnable.
 * - Papel de synchronized, wait(), notify(), sleep() e pools de threads.
 */
public class Ex10_Threads {

    // Crie suas classes aqui

    public static void main(String[] args) throws InterruptedException {
        // TAREFA 1

        // TAREFA 2

        // TAREFA 3

        // TAREFA 4

        // TAREFA 5

    }
}
